import { theme } from '../../theme';

export const defaultHeaderStyle = {
  backgroundColor: theme.colors.green,
};

export const defaultTabBarOptions = {
  tabStyle: {
    backgroundColor: theme.colors.blue,
  },
  activeTintColor: theme.colors.lightGrey,
  inactiveTintColor: theme.colors.darkGrey,
  indicatorStyle: { backgroundColor: theme.colors.green },
};
